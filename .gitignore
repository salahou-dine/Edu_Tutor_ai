# Environnement virtuel
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Secrets (à ne jamais publier)
.env
.env.*
*.key
*.pem
.streamlit/secrets.toml

# Logs
*.log
.streamlit-logs/

# OS / éditeurs
.DS_Store
.vscode/
.idea/

# Base vectorielle ChromaDB (régénérable, binaire)
data/vectorstore/

# Données utilisateur sensibles (jamais à publier) :
# - conversations.json contient les échanges des étudiants ;
# - last_tutor_prompt.md contient un prompt interne (debug).
data/conversations.json
data/conversations_web.json
data/users.json
data/.session_secret
data/last_tutor_prompt.md

# Caches multi-agents (régénérables) : artefacts IDP + contenus générés.
data/artifacts/
data/generated/
data/attachments/
data/vision_cache/

# Données runtime PAR UTILISATEUR (cours, artefacts, générés, conversations, PJ) :
# sensibles et régénérables — jamais versionnées.
data/users/
data/hermes_worker_*.log

# Config locale Claude Code
.claude/settings.local.json
